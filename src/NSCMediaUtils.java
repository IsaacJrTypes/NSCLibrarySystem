/**
 * NSCMediaUtils class allows for printing details of a generic item using a static method
 * */
public class NSCMediaUtils {
    public static void printMediaDetails(Artifact item) {
        System.out.println(item.toString());
    }
}